database:
  name: rental_dev
  password: rental

jwt:
  secret: dev-secret-not-for-production

cors:
  allow_origins:
    - "http://localhost:3000"
    - "http://localhost:5173"

storage:
  access_key: "minioadmin"
  secret_key: "minioadmin"
